[server]
headless = true
enableStaticServing = true

[browser]
gatherUsageStats = false

# Streamlit Cloud entrypoint MUST be streamlit_app.py (lowercase).
# Do not use Streamlit_app.py — removed from dev branch; uppercase breaks Linux deploys.
# App settings: repo Coakley11/baseball-stat-app · branch dev · not pinned to an old commit.
# deploy_epoch: 2026-08-02T05:35:00Z — redeploy pin 22ce3e3 (prod callback handoff); keep branch dev.
# deploy_epoch: 2026-08-02T02:08:00Z — redeploy pin 6d24920 (callback value-lifecycle observability).
# deploy_epoch: 2026-08-01T20:50Z — redeploy pin a2e6eb2 (auth-blocked restore preserve in-session room).
# deploy_epoch: 2026-08-01T20:20Z — redeploy pin 8f6c3fc (Stage-1 room latch observability).
# deploy_epoch: 2026-08-01T19:40Z — redeploy pin 8f431e2 (Stage-1 start pipeline observability).
# deploy_epoch: 2026-08-01T17:45Z — redeploy pin 05d80ae (register_widget metadata-at-registration emit).
# deploy_epoch: 2026-08-01T17:25Z — redeploy pin fdb045e (metadata-at-registration via registration hooks).
#
# Python runtime (required for stable native wheels):
#   Manage app → Settings → Python version → 3.12
# Streamlit runtime (required for width="stretch" dataframe/chart APIs):
#   requirements.txt pins streamlit==1.59.1
# Repository pins: .python-version, pyproject.toml requires-python, requirements.txt ranges.
# Python 3.14 on Cloud has caused uvicorn segfaults before app load — not a data-loss signal.
