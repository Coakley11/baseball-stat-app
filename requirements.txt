# deploy-bump: 2026-07-10-streamlit-159-api-compat (forces Streamlit Cloud full redeploy)
# Runtime: Python 3.12 — also set Streamlit Cloud → Manage app → Settings → Python version → 3.12
# Streamlit 1.59.x required for width="stretch" / width="content" dataframe and chart APIs.

setuptools>=65.5.0,<81

streamlit==1.59.1
# Pin Starlette for Streamlit 1.59 gzip middleware (GZipResponder thread_minimum_size); avoid 1.x resolver drift.
starlette==0.52.1
streamlit-sortables>=0.3.1,<0.5.0

numpy>=2.1.0,<2.3.0
pandas>=2.2.0,<2.3.0
pyarrow>=17.0.0,<20.0.0
scipy>=1.14.0,<1.16.0
scikit-learn>=1.5.0,<1.7.0
matplotlib>=3.8.0,<3.10.0

plotly>=5.24.0,<6.0.0
statsmodels>=0.14.0,<0.15.0
requests>=2.31.0,<3.0.0

pybaseball
openpyxl
xlsxwriter

supabase>=2.0.0,<3.0.0
